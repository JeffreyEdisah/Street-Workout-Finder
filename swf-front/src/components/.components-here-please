please add components in this folder :D

every component should look like this:

function MyComponentsName() {
  
  return (
    <div>
    // actual component stuff here
    </div>
  );
  }

export default MyComponentsName;